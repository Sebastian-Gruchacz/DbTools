<UserControl x:Class="Anonymyzer.Generators.Simple.Wpf.CompanyNameGeneratorEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="240" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Margin="0,0,0,14" TextWrapping="Wrap"
                   Text="Generates unique locale-shaped company names. The required marker keeps every result visibly synthetic." />

        <TextBlock Grid.Row="1" Margin="0,0,12,8" VerticalAlignment="Center" Text="Locale:" />
        <ComboBox x:Name="LocaleComboBox" Grid.Row="1" Grid.Column="1" Margin="0,0,0,8" IsEditable="True" />

        <TextBlock Grid.Row="2" Margin="0,0,12,8" VerticalAlignment="Center" Text="Synthetic marker:" />
        <TextBox x:Name="SyntheticMarkerTextBox" Grid.Row="2" Grid.Column="1" Margin="0,0,0,8" />

        <TextBlock Grid.Row="3" Margin="0,0,12,8" VerticalAlignment="Center" Text="Seed:" />
        <TextBox x:Name="SeedTextBox" Grid.Row="3" Grid.Column="1" Margin="0,0,0,8" />

        <CheckBox x:Name="IncludeLegalFormCheckBox" Grid.Row="4" Grid.ColumnSpan="2" Margin="0,0,0,8"
                  Content="Include legal form" />
        <CheckBox x:Name="PreserveNullsCheckBox" Grid.Row="5" Grid.ColumnSpan="2"
                  Content="Preserve null positions" />
    </Grid>
</UserControl>
