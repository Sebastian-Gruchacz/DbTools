<UserControl x:Class="Anonymyzer.Generators.Address.Wpf.PostalAddressGeneratorEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="220" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Margin="0,0,0,14" TextWrapping="Wrap"
                   Text="Generates country, region, city, street and postal code together from one locale-specific location record." />

        <TextBlock Grid.Row="1" Margin="0,0,12,8" VerticalAlignment="Center" Text="Locale:" />
        <ComboBox x:Name="LocaleComboBox" Grid.Row="1" Grid.Column="1" Margin="0,0,0,8" IsEditable="True" />

        <TextBlock Grid.Row="2" Margin="0,0,12,0" VerticalAlignment="Center" Text="Seed:" />
        <TextBox x:Name="SeedTextBox" Grid.Row="2" Grid.Column="1" />
    </Grid>
</UserControl>
