<Window x:Class="Anonymyzer.ConfigEditor.RescanOptionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Rescan detached clone"
        Width="500"
        Height="225"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Margin="0,0,0,14" TextWrapping="Wrap"
                   Text="The database name and detached-copy marker must match the open configuration. Existing tables, columns and operator choices are never deleted by a rescan." />

        <TextBlock Grid.Row="1" Margin="0,0,10,0" VerticalAlignment="Center" Text="Connection env:" />
        <TextBox x:Name="ConnectionEnvironmentTextBox" Grid.Row="1" Grid.Column="1" />

        <TextBlock Grid.Row="2" Grid.ColumnSpan="2" Margin="0,12,0,0" Foreground="DimGray" TextWrapping="Wrap"
                   Text="The connection string is read only from the environment and is not stored in the configuration." />

        <StackPanel Grid.Row="3" Grid.ColumnSpan="2" HorizontalAlignment="Right" Orientation="Horizontal">
            <Button Width="90" Margin="0,0,8,0" IsDefault="True" Content="Rescan" Click="Rescan_Click" />
            <Button Width="90" IsCancel="True" Content="Cancel" />
        </StackPanel>
    </Grid>
</Window>
